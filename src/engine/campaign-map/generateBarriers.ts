import type { CampaignBarrier, CampaignCell } from "./types";
export function generateBarriers(cells: CampaignCell[], width: number, height: number): { rivers: CampaignBarrier[]; mountainRanges: CampaignBarrier[] } { const rivers = cells.filter((cell) => cell.biome === "water").slice(0, Math.max(0, Math.floor(width / 3))).map((cell) => ({ ...cell, riverFlow: 1, tags: [...cell.tags, "river"] })).map((cell, index) => ({ id: `river-${index}`, path: [{ x: cell.x, y: cell.y }], width: 1, passLocations: [] })); const mountains = cells.filter((cell) => cell.biome === "mountains" || cell.biome === "snowPeaks").slice(0, Math.max(0, Math.floor(height / 3))).map((cell, index) => ({ id: `mountain-${index}`, path: [{ x: cell.x, y: cell.y }], width: 1, passLocations: [] })); return { rivers, mountainRanges: mountains }; }
