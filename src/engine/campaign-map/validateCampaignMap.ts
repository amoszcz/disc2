import type { GeneratedCampaignMap, MapValidationResult } from "./types";
export function validateCampaignMap(map: GeneratedCampaignMap): MapValidationResult { const errors: string[] = []; if (!map.locations.some((location) => location.type === "start")) errors.push("Map has no starting location."); if (map.cells.filter((cell) => cell.walkable).length === 0) errors.push("Map has no traversable cells."); if (map.locations.length < 2) errors.push("Map has fewer than two strategic locations."); return { valid: errors.length === 0, score: Math.max(0, 100 - errors.length * 25), errors, warnings: map.connections.length < 2 ? ["Map has limited route choices."] : [], metrics: { cells: map.cells.length, locations: map.locations.length, connections: map.connections.length, walkableCells: map.cells.filter((cell) => cell.walkable).length } }; }
